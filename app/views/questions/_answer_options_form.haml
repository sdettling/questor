= form_tag("/answers.json", :method => "post") do
  = hidden_field_tag "question_id", question.id
  - question.question_options.each do |option|
    = check_box_tag "question_option_id", option.id
    = label_tag option.id, option.description
  = submit_tag "Vote"

